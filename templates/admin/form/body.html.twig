{# templates/admin/form/body.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Admin - edit body{% endblock %}

{% block body %}
    <div class="row">
        {{form_start(form_edit)}}
        <p class="form-group">
            {{form_row(form_edit.title)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.slug)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.description)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.submit)}}
        </p>
        <p class="form-group">
            <a href="{{ path('admin_body_listing') }}">Back to listing</a>
        </p>
        {{form_end(form_edit)}}
    </div>
{% endblock %}
