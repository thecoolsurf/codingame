{# templates/admin/form/body.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Admin - edit body{% endblock %}

{% block body %}
    <div class="col-xs-12">
        {{form_start(form_edit)}}
        <p class="form-group">
            {{form_row(form_edit.title)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.slug)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.description)}}
        </p>
        {{form_end(form_edit)}}
    </div>
{% endblock %}
