{# templates/admin/form/page.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Admin - edit page{% endblock %}

{% block body %}
    <div class="row">
        {{form_start(form_edit)}}
        <p class="form-group">
            {{form_row(form_edit.h1)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.slug)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.submit)}}
        </p>
        <p class="form-group">
            <a href="{{ path('admin_page_listing') }}">Back to listing</a>
        </p>
        {{form_end(form_edit)}}
    </div>
{% endblock %}
