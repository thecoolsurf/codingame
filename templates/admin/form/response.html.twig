{# templates/admin/form/response.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Admin - edit response{% endblock %}

{% block body %}
    <div class="row" style="display: flow-root">
        <p><a href="{{ path('admin_response_listing') }}">Back to listing</a></p>
    </div>
    <div class="row">
        {{form_start(form_edit)}}
        <p class="form-group">
            {{form_row(form_edit.question.categories.title)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.answer)}}
        </p>
        <p class="form-group">
            {{form_row(form_edit.submit)}}
        </p>
        {{form_end(form_edit)}}
    </div>
    <div class="row" style="display: flow-root">
        <p><a href="{{ path('admin_response_listing') }}">Back to listing</a></p>
    </div>
{% endblock %}
